import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#1cf200",
      light: "#badec9",
    },
    secondary: {
      main: "#fc9d03",
    },
    otherColor: {
      main: "#999",
    },
  },
});
